import {Environment as DreiEnvironment,Lightformer} from '@react-three/drei';
import {useEditorStore} from '../../store/editorStore';
export default function Environment(){const intensity=useEditorStore(s=>s.lighting.environmentIntensity);const kind=useEditorStore(s=>s.lighting.environment);return <DreiEnvironment resolution={128} frames={1} environmentIntensity={intensity} key={kind}><color attach="background" args={['#3c3e43']}/><Lightformer intensity={3} position={[-3,2,4]} rotation={[0,.6,0]} scale={kind==='strip'?[1,8,1]:[4,7,1]}/><Lightformer intensity={2} position={[4,0,3]} rotation={[0,-.8,0]} scale={[1,7,1]}/><Lightformer intensity={2.5} position={[0,5,0]} rotation={[Math.PI/2,0,0]} scale={[5,5,1]}/><Lightformer intensity={kind==='softbox'?3:1} position={[0,0,-5]} scale={[4,6,1]}/></DreiEnvironment>}
