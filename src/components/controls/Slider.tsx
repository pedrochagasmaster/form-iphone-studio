import * as RadixSlider from '@radix-ui/react-slider';
import {useId} from 'react';
export default function Slider({label,value,min,max,step=.01,onChange,format}:{label:string;value:number;min:number;max:number;step?:number;onChange:(v:number)=>void;format?:(v:number)=>string}){const id=useId();return <div className="sliderControl"><div className="controlLabel"><label id={id}>{label}</label><output>{format?format(value):Number(value.toFixed(2))}</output></div><RadixSlider.Root className="slider" aria-labelledby={id} value={[value]} min={min} max={max} step={step} onValueChange={v=>onChange(v[0])}><RadixSlider.Track className="sliderTrack"><RadixSlider.Range className="sliderRange"/></RadixSlider.Track><RadixSlider.Thumb className="sliderThumb" aria-labelledby={id}/></RadixSlider.Root></div>}
